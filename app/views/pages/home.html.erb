<div class="background-color section-center">
    <div>
      <%= image_tag 'corps.png', class: 'img-nude' %>
    </div>

    <div>
      <h1 class="tagline">Sexplore</h1>
    </div>

    <div class="btn-blue-home">
      <% if user_signed_in? %>
        <%= link_to "Je commence", app_path, class: 'links' %>
      <% else %>
        <%= link_to "Je commence", explainer_one_path, class: 'links'%>
      <% end %>
    </div>
</div>
