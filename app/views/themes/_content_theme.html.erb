<div class='card-themes'>
  <h3>Choisi le thème qui te donne des frissons<h3>
  <% themes.each do |theme| %>
    <%= link_to theme_quizzes_path(theme) do %>
      <div class="card-theme">
        <%= image_tag theme.illustration, class: 'img-taille' %>
        <div class="card-theme-infos">
          <h4> <%= theme.name %> </h4>
          <p> <%= theme.description %> </p>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
