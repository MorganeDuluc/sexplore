<div class="background-color">

  <div class="card-search">
    <%= link_to app_path do %>
      <h1>Sexplore<h1>
    <% end %>
    <div class="card-chat-quiz">
      <p> Nous espérons que tu prêt(e)s à débloquer tous les niveaux 💪 </p>
      <%= image_tag 'informafion-1.png', class: 'img-fion-quiz' %>
    </div>
  </div>

  <div class="quiz-side">
    <h3>Bienvenue dans le thème: <strong><%= @theme.name %></strong></h3>
    <% @quizzes.each do |quiz| %>
    <% if quiz.finished == true %>
        <div class="card-quiz-unlock">
          <% if quiz.questions.present? %>
            <% link = question_path(quiz.questions.first) %>
          <% else %>
            <% link = root_path %>
          <% end %>

          <%= link_to link do %>

            <i class="fas fa-check"></i>
            <div class="card-quiz-unlock-infos">
              <h4> <%= quiz.title %> </h4>
              <p> 5 questions - 10 minutes </p>
            </div>
          <% end %>
        </div>
    <% else %>
      <div class="card-quiz-lock">
        <% if quiz.questions.present? %>
          <% link = question_path(quiz.questions.first) %>
        <% else %>
          <% link = root_path %>
        <% end %>



          <i class="fas fa-lock"></i>
          <div class="card-quiz-lock-infos">
            <h4> <%= quiz.title %> </h4>
            <p> 5 questions - 10 minutes </p>
          </div>

      </div>
    <% end %>
    <% end %>
  </div>
</div>
